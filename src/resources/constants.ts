interface Constants {
  campaignServerAddress: string;
}

const constants: Constants = {
  campaignServerAddress: "",
};

constants.campaignServerAddress = "https://localhost:7050";

export default constants;
