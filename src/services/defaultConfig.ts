import { IEndPointConfig } from "@/services/interfaces/IConfigService";

const defaultConfig: IEndPointConfig = {
  // Services
  baseUrlService: "https://localhost:7050",
};

export default defaultConfig;
